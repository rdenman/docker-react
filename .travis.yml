sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t rdenman/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true rdenman/docker-react npm run test
